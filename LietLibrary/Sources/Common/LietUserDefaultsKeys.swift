/// Central catalog of app-owned `UserDefaults` keys.
public enum LietUserDefaultsKeys {
    /// Keys stored in the shared App Group defaults suite.
    public enum AppGroup: String, CaseIterable {
        case batchImagePreferences = "B7q1N4xP"
        case batchBackgroundRemovalPreferences = "H3m8R2vK"
    }
}
